[package]
name = "Chess_Bot"
version = "0.1.0"
edition = "2018"

[dependencies]
chess = "3.2.0"
rusqlite = { version = "0.29.0", features = ["bundled"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tch = "0.6.0"  # Remove the 'features' part, just depend on tch directly
